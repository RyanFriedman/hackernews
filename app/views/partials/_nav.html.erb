<nav class="navbar navbar-default navbar-custom" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
       <%= link_to(root_path, :class => "navbar-brand navbar-brand-custom") do %>
	 	  <%= image_tag("logo1.png", :class => "logo") %>
		<% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
       	<li class="<%= active_link('/newest') %>"><%= link_to "New", newest_path %></li>
	    <li class="<%= active_link('/show') %>"><%= link_to "Show", show_path %></li>
	    <li class="<%= active_link('/ask') %>"><%= link_to "Ask", ask_path %></li>
	    <li class="<%= active_link('/leaders') %>"><%= link_to "Leaders", leaders_path %></li>
		<% if user_signed_in? %>
	      <li class="<%= active_link('/submit') %>"><%= link_to "Submit", submit_path %></li>
		<% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
	    <% if user_signed_in? %>
		  <li style="font-weight: bold;"><%= link_to "#{current_user.username} (#{current_user.karma})", user_path(current_user), :class => "user" %></li>
	      <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
	    <% else %>
	      <li style="font-weight: bold;"><%= link_to "Login", new_user_registration_path %></li>
	    <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>