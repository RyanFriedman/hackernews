<h3><%= @title %></h3>
<p><%= @description if @description %></p>

<ol>
  <% @posts.each_with_index do |post| %>
    <li class="post"><%= render :partial => "post", locals: { post: post } %></li>
  <% end %>
</ol>

<%= will_paginate @posts %>